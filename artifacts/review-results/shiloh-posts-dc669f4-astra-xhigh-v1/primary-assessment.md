# Primary assessment of the regimental/post comparison review

September 20, 2026. Reviewed input: `dc669f4ddb8388eea9c9029738f4f6b6e65c069c`.
The actual reviewer was `/root/shiloh_regimental_review`, `gpt-6-astra`, `xhigh`,
with fresh context. Its response and input bindings are preserved unmodified.

**Both required findings are accepted and closed by primary verification.**
SHILOH-RP-R1 restores `camps` for `changes` in `p503-camps`; SHILOH-RP-R2 restores
`Vol.` for `Vols.` twice in `p503-title`. The reviewer discovered both; the primary
independently inspected the full retained image and enlarged scale-6 title/caption
details from the parent PDF with SHA-256
`ced04dc52ad80568640bd53ba055d0d012c870787e5e0471e1e3ffa87e203a4e`, PDF p.533.
The direct retained image is `buell-posts-p503-facsimile-v1`, SHA-256
`0e38e2000330132030e03fcd310b32a870255cad12973bb99fc8eeb1eaf5699d`.

The [versioned correction manifest](../../../design/shiloh-regimental-posts-corrections-v1/source-corrections.json)
records exactly three literal replacements in `buell-battles-leaders-map-posts-v2`
and two RP06 anchor overlays. Exact-difference checks preserve all other text and
metadata except the version identity and correction provenance. All 21 anchors
replay with unchanged dates. The old source, prepared memo/record and original
review remain immutable. No second separate review of v2 is claimed.

Intake verified all 16 dispatch-bound file hashes, 7 assertion IDs, 21 exact
anchors, 19 distinct section bindings and 16 image bindings in the returned
record. The reviewer found 17 matching sections and two requiring literal
correction, not an estimated corpus accuracy rate. No additional nonblocking
comments were returned.

The independent primary preservation check compared the prior 133 source entries,
130 distinct raw paths and 109 protected files against base `af5075a`. It also
re-rendered all 13 new page images from the recorded parent PDFs/pages/scales;
all have identical pixels. The 23 prepared research/raw files are separately
bound in `prepared-files.json`; later status and correction documents do not
replace that packet. The correction preserves all 151 prepared registry entries
and adds one version, yielding 152 entries / 149 raw paths.

The bounded historical assessment is accepted with both literal findings closed.
The original review verdict remains `requires_literal_corrections`, recording
what the reviewer actually saw. Post identity, Saturday-to-Sunday continuity or
termination, exact first contact, area and populations remain unknown. Original
trial proceedings, Reid contributors, Medkirk manuscripts and map survey inputs
remain uninspected. The review reused prior metadata for Worthington's 1872 label
and the Medkirk volume's copyright year; it did not newly inspect those earlier
title/copyright images. Separate AI review does not authenticate testimony or
prove independent witnesses.

The final validation receipt records the checks and artifact hashes. No dossier,
cohort, admission proposal or baseline changed; no feature is admitted. The
baseline remains worse than equal odds and enriched-row promotion remains zero.
