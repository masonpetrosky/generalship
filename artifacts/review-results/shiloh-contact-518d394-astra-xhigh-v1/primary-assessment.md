# Primary assessment and literal correction closure

Date: 2026-09-20. Reviewed preparation:
`518d3941fb9227ea9d4f2f6d960fd9b80a45f648`.

The actual fresh-context Astra `xhigh` review inspected all 14 assertions,
38 passage anchors, 19 source/section pairs, 16 supplied page images and two
maps. Of the anchors, 34 were compared with new page images; the four existing
Beauregard p.386 anchors remained text-only. The reviewer also read all 16
retained OCR context pages, verified all 32 new source bindings, preserved
inputs, 82 tests and artifact reproduction. This purposive AI review does not
establish a historical error rate, source independence or historical truth.

Both findings are accepted and closed by exact versioned corrections:

- **CL-R1:** an independent primary inspection of an enlarged render of the
  original OR PDF p.301 (printed p.277) confirms the line-wrapped `com-` /
  `mandant`. `or-prentiss-opening-v2` changes only `commander thereof` to
  `commandant thereof`.
- **CL-R2:** an independent primary inspection of the original Frémaux JPEG
  legend confirms `Hornet nest`, without an added `s`.
  `loc-85690890-readings-v2` changes only that word.

All original source entries/files and actual review outputs are retained.
The [source correction manifest](../../../design/shiloh-contact-location-corrections-v1/source-corrections.json)
binds before/after bytes, metadata and review evidence. Its audit verifies
exact replacements, preservation of all 85 prior entries and replay of all
38 anchors under the explicit corrected-source substitutions. The existing
packet audit still passes against its preserved sources. The two v2 records
raise the registry to 87 entries / 84 raw paths without adding witnesses.

**Primary disposition: accepted as bounded, source-attributed research with
both requested literal corrections implemented.** There are no remaining
review-required corrections in this scope. The original review's
`corrections_required` decision remains immutable. A second reviewer run of
the corrected files is not claimed; closure rests on primary verification of
the exact edits requested by the separate reviewer.

All five historical gates stay open: engagement segmentation across April 3–5,
first qualifying April 6 contact, exact area and full location inventory,
afloat policy, and applicability of the seven blocked population observations.
No new dossier claims, quantities, events, model inputs or release manifests
are admitted. The old dossier remains 62 claims / 40 quantities / 26 events.
The complete 127-engagement / 36-campaign frame is retained. Both admission
ledgers and the unimproved 23-engagement / 13-group baseline remain unchanged.

The [final validation record](final-validation.json) records the completed
offline checks. The [primary preservation check](primary-preservation-check.json)
also records a successful replay of the older boundary audit at its exact
reviewed commit, rather than incorrectly asserting equality with today's registry.
